# Open Bastion SSH fingerprint spool, recreated at boot since /run is wiped.
# 0711 parent: the principals helper (nobody) reaches ssh-fp.sock but cannot list it.
d /run/open-bastion          0711 root root -
d /run/open-bastion/ssh-fp   0700 root root -
